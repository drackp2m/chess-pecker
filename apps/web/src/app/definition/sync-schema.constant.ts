/**
 * The syncable model this client understands, bumped by hand alongside the API's. Compared
 * before pushing: a server ahead of it means pull only, never push.
 */
export const SYNC_SCHEMA_VERSION = 1;
